{
  "rules": {
    "parties": {
      "$partyId": {
        ".read": true,
        ".write": "!data.exists() || data.child('passwordHash').val() === newData.child('passwordHash').val()"
      }
    }
  }
}
